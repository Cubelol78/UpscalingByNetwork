# FFmpeg portable pour traitement vidéo
imageio-ffmpeg>=0.4.9

# Chiffrement et sécurité
cryptography>=41.0.0

# Compression réseau (zstd prioritaire, zlib en fallback natif)
zstandard>=0.21.0

# Interface CLI
click>=8.1.7

# Utilitaires
pillow>=10.0.0

# Support AVIF pour optimisation transferts réseau
pillow-heif>=0.14.0

# Détection matérielle
psutil>=5.9.0

# Interface graphique
PyQt5>=5.15.0

# RAM disk Windows (optionnel, nécessite ImDisk Toolkit)
ramdisk; sys_platform == 'win32'

# Interface web (GUI accessible via navigateur)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.9
